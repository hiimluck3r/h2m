---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: littlelink
  annotations:
    konghq.com/strip-path: 'true'
spec:
  parentRefs:
  - name: kong
  hostnames:
  - 'littlelink.{{ domain }}'
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /
    backendRefs:
    - name: littlelink
      kind: Service
      port: 8002